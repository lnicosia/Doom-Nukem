#!bin/bash
echo "No install for Windows. Please install SDL2 and SDL2_ttf with all their needed dependencies yourself or compile them in ../lib. You will also probably need an X11 server (Xming for example)."
