#!bin/bash
echo "HomeBrew is needed for this install."
echo "This script was NOT tested. SDL2 and SDL2_ttf may still miss some other dependencies or the to-be-installed libraries may not even be needed for SDL2 and SDL2_ttf to work"
brew install freeglut
brew install freetype
