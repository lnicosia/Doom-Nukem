#!bin/bash
echo "HomeBrew is needed for this install."
echo "This script was NOT tested. Installed libraries may miss some dependencies and not work. If so, try install freeglut (brew install freeglut)."
brew install sdl2
brew install sdl2_ttf
